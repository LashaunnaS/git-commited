import styled from 'styled-components';

const ListItemStarsStyles = styled.span`
  color: var(--color-white-regular);
  padding: 0 0.5rem;
  font-size: var(--font-size-smaller);
`;

export default ListItemStarsStyles;
