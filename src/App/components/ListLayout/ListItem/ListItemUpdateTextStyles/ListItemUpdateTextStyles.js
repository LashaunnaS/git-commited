import styled from 'styled-components';

const ListItemUpdateTextStyles = styled.span`
  color: var(--color-purple-lighter);
  font-size: var(--font-size-smaller);
`;

export default ListItemUpdateTextStyles;
