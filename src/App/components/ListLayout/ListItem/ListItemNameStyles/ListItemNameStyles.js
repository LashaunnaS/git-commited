import styled from 'styled-components';

const ListItemNameStyles = styled.p`
  overflow: hidden;
  padding-bottom: 1rem;
  text-overflow: ellipsis;
  white-space: nowrap;
`;

export default ListItemNameStyles;
